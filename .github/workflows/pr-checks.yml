name: PR Checks

on:
	pull_request:
		branches: ["**"]
	workflow_dispatch:

jobs:
	pr-checks:
		runs-on: ubuntu-latest
		steps:
			- name: Checkout
				uses: actions/checkout@v4
			- name: Validate workflow
				run: echo "PR checks workflow configured."